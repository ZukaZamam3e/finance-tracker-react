export const Calendar = () => {
  const showAccounts = false;

  const sxBody = {
    display: !showAccounts ? "initial" : "none",
  };

  const body = <div style={sxBody}>aadad</div>;

  return body;
};
